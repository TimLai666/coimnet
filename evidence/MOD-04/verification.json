{
  "requirement": "MOD-04",
  "profile": "fixture",
  "reproduction_command": [
    "go test -count=1 -v -run 'Chemistry|Chemical|NeuralSource|ResourceAndFeedback|PlasticityAndChemistry' ./learning/",
    "go test -count=1 -v -run 'SourceSpec|ChemistryConfig|NeuralActivity' ./modulation/",
    "go test -count=1 -v -run 'Chemical' ./checkpoint/",
    "go test -count=1 ./...",
    "go test -race -count=1 ./modulation/ ./learning/ ./checkpoint/ ./dynamics/",
    "go list -deps ./modulation | grep TimLai666",
    "bash scripts/check-target-flow.sh",
    "go vet ./...",
    "gofmt -l ."
  ],
  "environment": [
    "go version go1.26.5 darwin/arm64, macOS 26.6.2 arm64",
    "Shared machine. uptime before the recorded runs was '19:46 up 11 days, 1:24, 1 user, load averages: 3.86 3.88 3.94' and after them '19:47 up 11 days, 1:25, 1 user, load averages: 6.70 4.58 4.19' (2026-09-16). Every timing here is a single sample under that load; no timing is reported as a result.",
    "Another agent was editing simulate/, internal/cli/, scripts/, ENG.md and README.md (ticket 19) in the same working tree while these commands ran; see limitations."
  ],
  "input_fingerprints": [
    "No external input. Every fixture is declared in learning/chemistry_test.go, modulation/config_test.go and checkpoint/individual_test.go.",
    "The expected tables were computed independently in CPython 3 (math.exp / math.tanh) before the Go code existed, and are written into learning/chemistry_test.go as decimal literals with the closed forms that produced them."
  ],
  "observed_result": "The chemical layer runs on the persistent individual in one fixed per-step order (source release, concentration, occupancy, effect arrays, the modulated core step, then the local plastic update when it is enabled), and every hand-computed table matches. (1) Continuous hand table, two isolated tanh neurons in two regions on one channel, dt = 1, tau = 2 (lambda = exp(-0.5) = 0.6065306597126334), an external timeline releasing 1 at step 1, one hypothesized receptor (Kd 0.5, n 1) on node 0 and one unresponsive receptor on node 1, a sensitivity effect gamma = 1 + occupancy, input 1 on node 0 every row: row 0 concentration 0, occupancy 0, gamma 1, node-0 voltage want 0.6321205588285577 got 0.6321205588285577; row 1 concentration want 0.7869386805747332 got 0.7869386805747332, occupancy want 0.611481100422978 got 0.6114811004229779, gamma want 1.6114811004229779 got 1.6114811004229779, voltage want 1.2511944916758613 got 1.2511944916758615; row 2 concentration want 0.4773024370823822 got 0.4773024370823822, occupancy want 0.48838764641507565 got 0.48838764641507565, gamma want 1.4883876464150756 got 1.4883876464150756, voltage want 1.401129161199922 got 1.4011291611999221; row 3 concentration want 0.289498562046025 got 0.289498562046025, occupancy want 0.3666866235902634 got 0.3666866235902634, gamma want 1.3666866235902635 got 1.3666866235902635, voltage want 1.379357325078631 got 1.379357325078631. Every difference is at most one unit in the last place and every comparison is within 1e-12; node 1 stays at a bit-exact zero voltage on every row and the unresponsive receptor reports a declared 0 with status unresponsive, counted as {assumed 0, unknown_skipped 0, unresponsive 1} and never as a measured zero. One four-row call reproduces the same final concentration, occupancy and voltage as four one-row calls and reports release total [1] for the single pulse. (2) LIF hand table, the same chemistry with a threshold effect m = 0.5*occupancy (theta_abs_max 1) on the same two-node fixture, theta_min 0.1, theta_max 2, theta_raw 0 so theta_base is exactly 1.05, input 2 on node 0 every row: row 0 shift 0, effective threshold 1.05, candidate 1.2642411176571153, spike, voltage -0.5; row 1 shift want 0.305740550211489 got 0.30574055021148894, effective threshold 1.355740550211489, candidate 1.0803013970713942, silent, voltage want 1.0803013970713942 got 1.0803013970713942; row 2 shift want 0.24419382320753782 got 0.24419382320753782, effective threshold 1.2941938232075378, candidate 1.6616617919084682, spike, voltage -0.5; row 3 shift want 0.1833433117951317 got 0.1833433117951317, effective threshold 1.2333433117951318, candidate 1.0803013970713942, silent, voltage want 1.0803013970713942 got 1.0803013970713942. The same fixture without the effect spikes on all four rows (every candidate clears 1.05 and every row ends at the reset -0.5), which is asserted in the test, so the effect is what suppresses the events of rows 1 and 3. (3) Disabled and neutral are the old path, bit for bit, on both cores: enabling and then disabling leaves the outputs and the voltage bit-identical to an individual that never enabled the layer, and a declared chemistry whose source releases nothing keeps occupancy at exactly 0 with no clamp and the same bit-identical outputs and voltage. A disabled individual reports the zero-valued ChemistryReport. (4) Temporary effects never reach Parameters: 100 rows on the spiking core under a pulse every third step, with a live occupancy at the end, leave weights, bias, log_tau, theta_raw, encoder and readout bit-identical (compared as float64 bit patterns, so a sign flip on a zero would fail). (5) Feedback that has not arrived is never handed to a source: a feedback available at step 3 is queued, steps 0 to 2 advance successfully and step 3 advances successfully, while the same source handed the same unfiltered queue at step 0 refuses the call, which is what makes the successful run evidence rather than a silent skip. OfferFeedback refuses a feedback on any clock other than model_step. (6) The activity a source averages over is the previous step's node outputs: a neural source refuses the very first row of a fresh individual rather than inventing a zero, and after one warm-up step it releases exactly that step's node-0 output tanh(1-exp(-1)) = 0.5595106570525964, which one step of the declared kinetics turns into 0.44030057822847224. (7) A declared resource reaches the chemistry through the declared rule only: with no resource declared the source refuses and the advance commits no step, and after SetResource('energy', 0.75) the rule 2*max(0.75-0.25, 0) = 1 gives the same concentration 0.7869386805747332. (8) Snapshot: an individual with a transport matrix, a resource and a queued feedback round-trips through JSON and RestoreIndividual with a bit-identical chemical part, and the snapshot shares no buffer with the running individual. A mid-run snapshot continues exactly: three rows, a JSON snapshot, a restore and three more rows reproduce the uninterrupted six-row run bit for bit in outputs, concentration, occupancy, release total and voltage, under a neural source whose input is the previous row's activity, which no field of the chemical part stores. RestoreIndividual refuses eleven broken chemical parts (a concentration with the wrong region count, a negative or non-finite concentration, a region map for the wrong node count, a missing source, a receptor without a mapping version, an effect naming a receptor that does not exist, a non-finite resource, a blank resource name, feedback on another clock and invalid feedback). (9) checkpoint treats the part as optional exactly like plastic and accumulator: an individual that never enabled the layer writes no 'chemical' key, an absent key loads as disabled and changes nothing else, an enabled one round-trips through SaveIndividual and LoadIndividual bit-identically with no null anywhere in the document, the loaded part is not aliased with a later caller mutation, and sixteen hand-edited blocks are refused (a null part, each of the four missing fields, a null or shape-broken state, a missing concentration or steps, a null inside the concentration, a config without its chemistry or regions, a receptor whose mapping_version key is misspelled, feedback without a source or on another clock, and a non-finite resource). (10) The two mechanisms compose: with local plasticity enabled, a silent chemistry leaves the outputs, the plastic report and the fast state bit-identical to the plastic-only run while both reports cover all five rows, and the same declaration with its pulse changes the outputs, so the composition is not vacuous. (11) The import-cycle fix: modulation.NeuralActivity now carries explicit ascending node indices and a label, so go list -deps ./modulation lists only internal/jsonkey, internal/strictjson and signal, no simulate and no connectome, which is what lets learning import modulation without an import cycle in the connectome test binary. scripts/check-target-flow.sh still passes and the ticket-20 hand-computed release numbers are unchanged. 43 test functions and subtests passed across the three packages, the race detector reported nothing, go test -count=1 ./... was green in every package, go vet was clean and gofmt -l . printed nothing.",
  "assumption_statement": "Fixture only: hand-declared chemistry, receptors, regions, effect coefficients and sources, with no biological claim about which cell carries which receptor or what a channel is. Kd and N are engineering coefficients in every status and their JSON names say so. An unresponsive receptor's zero is a declared absence of response and is counted separately from a measured one. Sources are global to every region in this stage: a channel's release rate is added to every region, and per-region sources are a later decision. The modulated input current is the external input plus the delayed synaptic contribution; the bias stays outside the gain. The threshold floor is applied only where the shift is non-zero, which keeps an unmodulated step bit for bit intact. The clock is the individual's own model step: the step a source releases for and the 'now' feedback is filtered against are both the persistent neural state's step count, so a snapshot carries them without a separate field. The activity a source reads is the previous step's node outputs, read back from the persistent delay history, and is absent before the first step rather than replaced by a zero. The unit string of a chemistry is a declaration and the framework performs no conversion.",
  "test_log": [
    "evidence/MOD-04/red-config.log (first failing output: the modulation configuration tests before modulation/config.go existed, 'undefined: ChemistryConfig', 'undefined: SourceSpec', 'undefined: RegionAssignment')",
    "evidence/MOD-04/red-individual.log (first failing output: the individual tests before the chemical layer existed, 'EnableChemistry undefined', 'ChemistryReport undefined', 'DisableChemistry undefined', 'OfferFeedback undefined')",
    "evidence/MOD-04/red-snapshot.log (first failing output of the checkpoint optional-part tests before requireIndividualChemical existed: three hand-edited chemical blocks with a missing 'steps', 'resources' or 'pending_feedback' were accepted)",
    "evidence/MOD-04/test.log (the passing verbose runs of the three packages, the whole-repository run, the race runs, the modulation dependency list, scripts/check-target-flow.sh, go vet and gofmt, with go version and uptime before and after)"
  ],
  "reports": [
    "docs/tickets/21-chemical-state-receptors-and-effects.md (the stage-two record and the deviations)",
    "docs/individual-state.md (the chemical block of a persistent individual)",
    "docs/model-and-mechanisms.md (the three mechanism rows)"
  ],
  "limitations": [
    "Sources are global to all regions. Every channel's release rate is added to every region, so regional structure enters only through transport and through the region map, never through where a source releases.",
    "Forward and backward are unmodulated. Continuous.Forward, LIF.Forward and the whole reverse pass are untouched, so an episode gradient step sees no chemistry; the differentiable modulation path (MOD-07) is ticket 22. TrainEpisode therefore trains a model the persistent path no longer runs exactly, which is the same split local plasticity already has.",
    "Fixture scale only: at most three nodes, two regions, one channel and one hundred steps. No whole-brain, real-data or cross-platform run, and no performance measurement.",
    "The three occupancy counts in ChemistryReport describe the last row's records, matching the Occupancy field, while the three clamp counts are summed over the rows of the call. That split is a decision of this stage, not a line of the ticket.",
    "The feedback queue is never drained. None of the four implemented sources reads a score, so keeping an arrived feedback queued changes no number, but a caller that offers feedback every step grows the queue and therefore the snapshot.",
    "SetResource and OfferFeedback require an enabled chemistry. Nothing else in an individual reads them and the snapshot has nowhere to keep them while the layer is off, so they are refused rather than silently dropped at the next save.",
    "A threshold effect on a continuous individual is refused at the step where the shift first becomes non-zero, by the continuous core, and not at EnableChemistry. Refusing it earlier would break the neutral-equals-off property, because a declared threshold effect at zero concentration is exactly the unmodulated run.",
    "The chemical part of the learning snapshot was implemented in the same change as the individual integration and its tests were written immediately after it, not before it. The checkpoint half was written test first and its red state is recorded in red-snapshot.log.",
    "The working tree was shared: another agent was editing simulate/, internal/cli/, scripts/, ENG.md and README.md (ticket 19) while this work ran, so a rerun on a clean checkout of this ticket alone has not been done. Every command recorded here passed with that uncommitted work in the tree."
  ]
}
